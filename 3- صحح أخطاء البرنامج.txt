**صحح أخطاء البرنامج**

صحح الأخطاء الموجودة في البرامج التالية:

1.

صحح الأخطاء الموجودة في البرنامج التالي، والذي يأخذ كدخل عددين صحيحين ثم يقوم بحساب مجموعهما وباقي قسمة الأول على الثاني.

```cpp
#include <iostream.h>

main()
{
    char x, y;

    cin >> x >> y;

    cin << "Sum=" << x+y << "\n"

    cout << "Mod=" << x/y << "\n";

    return 0;
}
```

---

2.

صحح الأخطاء الموجودة في البرنامج التالي، والذي يقوم بحساب مجموع الأعداد الزوجية الموجودة في المجال من 15 إلى 25.

```cpp
#include <iostream.h>

main()
{
    bool sum = 0;

    for (int i = 1; i <= 15; i--)
    {
        if (i / 2 = 0);
            sum = sum + 1;
    }

    cin << "sum is : " << i;

    return 0;
}
```

---

3.

صحح الأخطاء الموجودة في البرنامج التالي، والذي يقوم بحساب مجموع الأعداد الزوجية ضمن المجال من `1` إلى `30`.

```cpp
#include <math.h>

main()
{
    int sum = 1;

    for (int i = 1; i <= 10; i++)
    {
        if (i / 2 == 0)
            sum = sum + 1;
    }

    cout << "sum is : " << i;

    return 0;
}
```

---

4.

صحح الأخطاء الموجودة في البرنامج التالي، والذي يقوم بقراءة نصف قطر دائرة ثم يطبع قيمة محيطها ومساحتها.

```cpp
#include <iostream.h>

main()
{
    bool r;

    double s, m;

    double p = 3.14;

    cin >> r;

    m = 2 * p * r;

    s = p * r * r;

    cin >> "m = " >> m >> "\n";

    cout << "s = " << s << "\n";

    return 0;
}
```

---

5.

صحح الأخطاء الموجودة في البرنامج التالي، والذي يقوم بحساب مجموع الأعداد من `30` إلى `50`.

```cpp
#include <iostream.h>

main()
{
    char sum = 0;

    while (i = 1; i++; i <= 20)
    {
        sum == sum + 0;
    }

    cin << "sum is" << i;

    return 0;
}
```

---

6.

صحح الأخطاء الموجودة في البرنامج التالي، والذي يقوم بقراءة `3` أعداد، ثم يطبع مجموعها ومتوسطها الحسابي.

```cpp
include <iostream.h>

in()
{
    float x1, x2, x3;

    cout >> x1 >> x2 >> x3;

    cin << x1 + x2 + x3

    cout < x1 + x2 + x3 / 3;

    return 0;
}
```

---

7.

صحح الأخطاء الموجودة في البرنامج التالي، والذي يقوم بقراءة نصف قطر دائرة، ثم يطبع قيمة قطرها ومحيطها ومساحتها.

```cpp
#include <iostream.h>

main()
{
    bool r;

    float p = 3.14;

    cin >> r

    cin << r * 2 << "\n";

    cout << 2 * p * R << "\n";

    cout << p * r * r << "\n";

    return 0;
}
```

---

8.

صحح الأخطاء الموجودة في البرنامج التالي، والذي يقوم بحساب المتوسط الحسابي للأعداد الموجودة في المجال من `1` إلى `10`.

```cpp
#include <iostream.h>

main()
{
    int sum = 1;

    for (int i = 1; i <= 10; i--)
    {
        sum = sum + 1;
    }

    cout << "avg = " << i;

    return 0;
}
```
