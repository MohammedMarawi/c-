**السؤال 1:**
أجب بصح أو خطأ مما يلي، مع تصحيح العبارة الخاطئة:

1. نستخدم الرمز `\t` للانتقال إلى سطر جديد.
2. التعليمة التالية تستخدم للتصريح عن متحول عشري:

   ```cpp
   bool x;
   ```
3. عند كتابة الخوارزمية بالطريقة التدفقية نستخدم رمز المعين لعمليات الإدخال والإخراج.
4. نستخدم التعليمة `cout` لقراءة قيمة من المستخدم.
5. يمكن لاسم المتحول في `C++` أن يبدأ برقم.

---

**السؤال 2:**
اكتب بالطريقة البيانية، أي الخوارزمية البيانية، الخوارزمية اللازمة لإدخال عدد صحيح `y` وطباعة العبارة `Positive` إذا كان موجبًا والعبارة `Negative` إذا كان سالبًا.

---

**السؤال 3:**
صحح الأخطاء الموجودة في البرنامج التالي، والذي يأخذ كدخل عددين صحيحين ثم يقوم بحساب مجموعهما وباقي قسمة الأول على الثاني.

```cpp
#include <iostream.h>

main()
{
    char x, y;

    cin >> x >> y;

    cin << "Sum=" << x+y << "\n"

    cout << "Mod=" << x/y << "\n";

    return 0;
}
```

---

**السؤال 4:**
اكتب برنامجًا يأخذ كدخل علامات طالب في 3 مواد، ثم يطبع على الشاشة مجموع العلامات والمعدل، أي المتوسط الحسابي.

---

**السؤال 5:**
اختر الإجابة الصحيحة لكل مما يلي:

1. التصريح عن متحول من نوع عدد عشري يكون بالشكل:

   أ.

   ```cpp
   int x = 5.7;
   ```

   ب.

   ```cpp
   int x = 5;
   ```

   ج.

   ```cpp
   float x = 5.7;
   ```

2. ناتج تنفيذ العملية الحسابية التالية:

   ```cpp
   3 + 1 * (6 - 6) / 2
   ```

   أ. `6`
   ب. `9`
   ج. `21`

3. التعليمة التي تؤدي إلى تنفيذ مجموعة من التعليمات عددًا محددًا من المرات هي:

   أ. `for`
   ب. `while`
   ج. `cin`

4. تعليمة الطباعة على الشاشة هي:

   أ. `print`
   ب. `cin`
   ج. `cout`

5. بفرض:

   ```cpp
   x = 11
   y = 3
   z = x % y
   ```

   فإن قيمة `z` هي:

   أ. `1`
   ب. `2.5`
   ج. `2`

---

**السؤال 6:**
صحح الأخطاء الموجودة في البرنامج التالي، والذي يقوم بحساب مجموع الأعداد الزوجية الموجودة في المجال من 15 إلى 25.

```cpp
#include <iostream.h>

main()
{
    bool sum = 0;

    for (int i = 1; i <= 15; i--)
    {
        if (i / 2 = 0);
            sum = sum + 1;
    }

    cin << "sum is : " << i;

    return 0;
}
```

---

**السؤال 7:**
اكتب نتيجة تنفيذ كلٍّ من المقاطع البرمجية التالية:

1.

```cpp
int z = 8;
int y = 6;

if (z <= 10 && y > 5)
    cout << "values are between 5 and 10";
else
    cout << "wrong value";
```

2.

```cpp
for (int i = 1; i <= 10; i++)
{
    if (i % 2 == 0)
        cout << i << "\n";
}
```

3.

```cpp
int s = 5;

s = s + 2 * 3 - 11;

cout << "value is: " << s;
```

4.

```cpp
int x = 9;

cout << x++ << "\n";

cout << x;
```

---

**السؤال 8:**
اكتب برنامجًا يطلب من المستخدم إدخال علامات طالب في 5 مواد، ثم يقوم بحساب معدل هذا الطالب، فإذا كان المعدل أعلى من 70 يطبع على الشاشة العبارة `Very Good`، وإلا يطبع العبارة `Accepted`.

---

**السؤال 9:**
اكتب الخوارزمية اللازمة لحساب ناتج:

```cpp
y = x / (x - 3)
```

بالطريقة البيانية، أي الخوارزمية البيانية.
**السؤال 10:**
أجب بـ `صح` أو `خطأ` عن كل مما يلي، مع تصحيح العبارات الخاطئة:

1. للتصريح عن متحول من نمط عدد صحيح نكتب:

   ```cpp
   int a = 3;
   ```

2. تعليمة الطباعة على الشاشة هي:

   ```cpp
   for
   ```

3. لنتمكن من استخدام الدوال الرياضية يجب تضمين الملف الرأسي:

   ```cpp
   <iostream.h>
   ```

4. بفرض:

   ```cpp
   a = 18
   b = 3
   c = a % b
   ```

   فإن قيمة `c` هي `6`.

5. نستخدم التعليمة `if` لتنفيذ مجموعة من التعليمات عدة مرات.

6. بفرض:

   ```cpp
   a = 5
   a *= 10
   ```

   فإن قيمة `a` هي `15`.

7. الدالة الرياضية `sqrt(x)` تستخدم لإيجاد الجذر التربيعي للعدد `x`.

---

**السؤال 11:**
صحح الأخطاء الموجودة في البرنامج التالي، والذي يقوم بحساب مجموع الأعداد الزوجية ضمن المجال من `1` إلى `30`.

```cpp
#include <math.h>

main()
{
    int sum = 1;

    for (int i = 1; i <= 10; i++)
    {
        if (i / 2 == 0)
            sum = sum + 1;
    }

    cout << "sum is : " << i;

    return 0;
}
```

---

**السؤال 12:**
اكتب نتيجة تنفيذ كل من المقاطع البرمجية التالية:

1.

```cpp
for (int i = 1; i <= 5; i++)
    cout << i << endl;
```

2.

```cpp
for (int i = 5; i >= 0; i--)
{
    if (i % 2 == 0)
        cout << i << "\n";
}
```

3.

```cpp
int n = 3, d = 2000;

switch (n)
{
    case 0:
        cout << d;
        break;

    case 1:
        cout << d + 100;
        break;

    default:
        cout << d + 100 + (n - 2) * 500;
        break;
}
```

4.

```cpp
int x = 5, y = 3;

if (x >= 2 && y > 5)
    cout << "val-" << x * y;
else
    cout << "value -" << x + y;
```

5.

```cpp
int a = 10;

cout << a-- << "\t";

cout << a;
```

6.

```cpp
int i = 1, x = 0;

while (i <= 10)
{
    x = x + i;
    i++;
}

cout << "Sum = " << x;
```

---

**السؤال 13:**
اكتب برنامجًا باستخدام تعليمة `if` يسمح للمستخدم بإدخال علاماته في ستة مقررات، ثم يقوم بحساب المعدل، فإذا كان المعدل أعلى من `80` يطبع على الشاشة العبارة `Good Level`، وإذا كان أصغر يطبع على الشاشة العبارة `You should work hard`.
**السؤال 14:**
أجب بـ `صح` أو `خطأ` عما يلي، مع تصحيح العبارة الخاطئة:

1. نستخدم الرمز `\t` للانتقال إلى سطر جديد.
2. التعليمة التالية تُستخدم للتصريح عن متحول من نوع عدد صحيح:

   ```cpp
   bool x;
   ```
3. عند كتابة الخوارزمية بالطريقة البيانية نستخدم رمز المعين لعمليات المعالجة.
4. نستخدم التعليمة `cout` لقراءة قيمة من المستخدم.
5. يمكن لاسم المتحول في `C++` أن يبدأ برقم.

---

**السؤال 15:**
اكتب بالطريقة البيانية الخوارزمية اللازمة لإدخال عدد صحيح `y`، ثم طباعة ناتج العملية:

```cpp
1 / (y - 7)
```

---

**السؤال 16:**
صحح الأخطاء الموجودة في البرنامج التالي، والذي يأخذ كدخل عددين صحيحين ثم يطبع مجموعهما وباقي قسمة الأول على الثاني.

```cpp
#include <iostream.h>

main()
{
    char x, y;

    cin >> x >> y;

    cin << x + y << "\n"

    cout << x / y << "\n";

    return 0;
}
```

---

**السؤال 17:**
اكتب برنامجًا يطلب من المستخدم إدخال أسعار `3` مواد غذائية، ثم يطبع على الشاشة مجموع أسعار هذه المواد والمتوسط الحسابي لهذه الأسعار.

---

**السؤال 18:**
أجب بـ `صح` أو `خطأ` عن كل مما يلي، مع تصحيح العبارات الخاطئة:

1. يمكن أن يكون اسم المتحول بالشكل التالي:

   ```cpp
   f@m
   ```

2. تعليمة الطباعة على الشاشة هي:

   ```cpp
   cin
   ```

3. للتصريح عن متحول من نمط عدد صحيح نستخدم النمط:

   ```cpp
   char
   ```

4. تعتبر لغة `C++` حساسة لحالة الأحرف، أي تميز بين الأحرف الكبيرة والصغيرة.

5. النمط `int` يتم حجزه في الذاكرة بالحجم `1 byte`.

6. بفرض:

   ```cpp
   a = 10
   b = 3
   c = a % b
   ```

   فإن قيمة `c` هي `3`.

7. نستخدم التعليمة `if` لتنفيذ مجموعة من التعليمات إذا تحقق شرط معين.

8. بفرض:

   ```cpp
   a = 2
   a += 10
   ```

   فإن قيمة `a` هي `10`.

9. ناتج تنفيذ العملية الحسابية التالية هو `6`:

   ```cpp
   6 + 2 * 4 - 8 / 4
   ```

10. لكتابة تعليق يحتوي على أكثر من سطر نكتب:

```cpp
//
```

---

**السؤال 19:**
صحح الأخطاء الموجودة في البرنامج التالي، والذي يقوم بقراءة نصف قطر دائرة ثم يطبع قيمة محيطها ومساحتها.

```cpp
#include <iostream.h>

main()
{
    bool r;

    double s, m;

    double p = 3.14;

    cin >> r;

    m = 2 * p * r;

    s = p * r * r;

    cin >> "m = " >> m >> "\n";

    cout << "s = " << s << "\n";

    return 0;
}
```

---

**السؤال 20:**
اكتبي الخوارزمية النصية والخوارزمية البيانية لحساب محيط ومساحة مربع، طول ضلعه `x`.

---

**السؤال 21:**
اكتب نتيجة تنفيذ كلٍّ من المقاطع البرمجية التالية:

1.

```cpp
int x = 10, y = 8, z;

z = x + y;

cout << "x + y = \t" << z;
```

2.

```cpp
int a = 13;

cout << a << endl;

cout << ++a << endl;

cout << a << endl;
```

3.

```cpp
int a = 4, b;

b = a - 10 / 2;

cout << b << "\n";

if (b >= 0)
    cout << "postitive" << "\n";
else
    cout << "Negative" << "\n";
```

4.

```cpp
int z = 8;

cout << z << "\n";

cout << z-- << "\n";

cout << z << "\n";
```
**السؤال 22:**
أجب بـ `صح` أو `خطأ` لكل من العبارات التالية، مع تصحيح العبارات الخاطئة:

1. بفرض:

   ```cpp
   x = 7
   y = 3
   z = x % y
   ```

   فإن قيمة `z` هي `2`.

2. التصريح عن متحول من نوع عدد عشري يكون بالشكل:

   ```cpp
   char x = 5.7;
   ```

3. نتيجة العملية الحسابية التالية هي `22`:

   ```cpp
   6 + 8 / 2 + 4 * 2
   ```

4. التعليمة التي تؤدي إلى تنفيذ مجموعة من التعليمات عددًا من المرات هي:

   ```cpp
   if
   ```

5. تعليمة القراءة والإدخال هي:

   ```cpp
   while
   ```

---

**السؤال 23:**
صحح الأخطاء الموجودة في البرنامج التالي، والذي يقوم بحساب مجموع الأعداد من `30` إلى `50`.

```cpp
#include <iostream.h>

main()
{
    char sum = 0;

    while (i = 1; i++; i <= 20)
    {
        sum == sum + 0;
    }

    cin << "sum is" << i;

    return 0;
}
```

---

**السؤال 24:**
اكتب بالطريقة البيانية الخوارزمية اللازمة لقراءة علامة طالب `mark`، فإذا كانت العلامة ناجحة يطبع العبارة `passed`، وإذا كانت راسبة يطبع العبارة `failed`.

---

**السؤال 25:**
اكتب نتيجة تنفيذ كلٍّ من المقاطع البرمجية التالية:

1.

```cpp
int x = 4, y = 11;

if (x % 2 == 0 && y > 10)
    cout << "value is" << x + y;
else
    cout << "The Val =" << x * y;
```

2.

```cpp
for (int i = 10; i < 15; i++)
{
    if (i == 13)
        break;

    cout << i << endl;
}
```

3.

```cpp
for (int i = 6; i > 1; i--)
{
    if (i % 2 != 0)
        continue;

    cout << i << "\t" << i * i << endl;
}
```

4.

```cpp
int num = 10;

cout << num++ << "\n";

cout << num;
```

---

**السؤال 26:**
اكتب برنامجًا يطلب من المستخدم إدخال `10` أعداد صحيحة، ثم يقوم بحساب ناتج جمع هذه الأعداد، فإذا كان الناتج أكبر تمامًا من `200` يطبع العبارة `Between 100 and 200`، وإذا كان يساوي `500` يطبع العبارة `Equal 500`، وإذا كان غير ذلك يطبع العبارة `Try again`.

---

**السؤال 27:**
اختر الإجابة الصحيحة لكل مما يلي:

1. التصريح عن متحول من نوع عدد عشري يكون بالشكل:

   أ.

   ```cpp
   int y = 5.7;
   ```

   ب.

   ```cpp
   int y = 5;
   ```

   ج.

   ```cpp
   float y = 5.7;
   ```

2. ناتج تنفيذ العملية الحسابية التالية:

   ```cpp
   5 + 2 * 6 - 6 % 2
   ```

   أ. `39`
   ب. `4`
   ج. `17`

3. التعليمة التي تؤدي إلى تنفيذ مجموعة من التعليمات عند تحقق شرط معين:

   أ. `main`
   ب. `cin`
   ج. `if`

4. تعليمة الطباعة على الشاشة هي:

   أ. `print`
   ب. `cin`
   ج. `cout`

5. اسم المتحول في `C++` يجب:

   أ. أن يبدأ برقم.
   ب. ألا يتضمن فراغات.
   ج. أن يكون من الأسماء المحجوزة في اللغة.

---

**السؤال 28:**
صحح الأخطاء الموجودة في البرنامج التالي، والذي يقوم بقراءة مجموع `3` أعداد صحيحة ومتوسطها الحسابي.

```cpp
include <iostream.h>

in()
{
    float x1, x2, x3;

    cout >> x1 >> x2 >> x3;

    cin << x1 + x2 + x3

    cout < x1 + x2 + x3 / 3;

    return 0;
}
```

---

**السؤال 29:**
اكتب نتيجة تنفيذ كلٍّ من المقاطع البرمجية التالية:

1.

```cpp
int z = 5, y = 10;

if (z <= 8 && y < 5)
    cout << "values are between 0 and 10";
else
    cout << "wrong value";
```

2.

```cpp
int s = 3;

s = s + 2 * 3 - 9;

cout << "value is: " << s;
```

3.

```cpp
int y = 8;

cout << --y << "\n";

cout << y;
```

4.

```cpp
int x = 11;

cout << x++ << "\n";

cout << x;
```

---

**السؤال 30:**
اكتب الخوارزمية اللازمة لحساب:

```cpp
y = x / (x - 3)
```

بالطريقة البيانية، أي الخوارزمية البيانية.

---

**السؤال 31:**
اكتب برنامجًا لإدخال عددين صحيحين وحساب ناتج ضربهما، فإذا كان ناتج الضرب عددًا زوجيًا تتم طباعة العبارة `even value` على الشاشة، وإلا تتم طباعة العبارة `odd value` على الشاشة.

---

**السؤال 32:**
أجب بـ `صح` أو `خطأ` عن كل مما يلي، مع تصحيح العبارات الخاطئة:

1. للتصريح عن متحول من نمط عدد صحيح نكتب:

   ```cpp
   int a = 3;
   ```

2. تعليمة الطباعة على الشاشة هي:

   ```cpp
   for
   ```

3. لنتمكن من استخدام الدوال الرياضية يجب تضمين الملف الرأسي:

   ```cpp
   <iostream.h>
   ```

4. بفرض:

   ```cpp
   a = 18
   b = 3
   c = a % b
   ```

   فإن قيمة `c` هي `6`.

5. نستخدم التعليمة `if` لتنفيذ مجموعة من التعليمات عدة مرات.

6. بفرض:

   ```cpp
   a = 5
   a *= 10
   ```

   فإن قيمة `a` هي `15`.

7. الدالة الرياضية `sqrt(x)` تستخدم لإيجاد الجذر التربيعي للعدد `x`.

---

**السؤال 33:**
صحح الأخطاء الموجودة في البرنامج التالي، والذي يقوم بحساب مجموع الأعداد الزوجية ضمن المجال من `1` إلى `30`.

```cpp
#include <math.h>

main()
{
    int sum = 1;

    for (int i = 1; i <= 10; i++)
    {
        if (i / 2 == 0)
            sum = sum + 1;
    }

    cout << "sum is :" << i;

    return 0;
}
```

---

**السؤال 34:**
اكتب نتيجة تنفيذ كلٍّ من المقاطع البرمجية التالية:

1.

```cpp
int s = 16, m = 2;

if (s >= 4 && m > 10)
    cout << "result =" << pow(m, 5);
else
    cout << "result =" << pow(m, 3);
```

2.

```cpp
for (int i = 1; i <= 20; i++)
{
    if (i % 2 != 0)
        continue;

    cout << i << "\n";
}
```

3.

```cpp
int i = 1;

while (i <= 5)
{
    cout << i << "\t";

    i++;
}
```

4.

```cpp
int z = 20;

cout << z-- << "\n";

cout << z << "\n";
```

5.

```cpp
int n = 4, d;

d = 6 + n / 2;

switch (d)
{
    case 5:
        cout << d * 10;
        break;

    case 8:
        cout << d * 100;
        break;

    default:
        cout << "Error";
        break;
}
```

6.

```cpp
for (int i = 5; i <= 10; i++)
{
    if (i == 7)
        break;

    cout << i << endl;
}
```

---

**السؤال 35:**
اكتب برنامجًا يطلب من المستخدم إدخال أسعار `5` مواد غذائية، ثم يقوم بحساب مجموع ومتوسط أسعار هذه المواد.

---

**السؤال 36:**
اكتب الخوارزمية النصية والخوارزمية البيانية لإدخال `x`، وإيجاد قيمة:

```cpp
y = x / (x - 7)
```
**السؤال 37:**
أجب بـ `صح` أو `خطأ` لكل من العبارات التالية، مع تصحيح العبارات الخاطئة:

1. بفرض:

   ```cpp
   x = 7
   y = 3
   z = x % y
   ```

   فإن قيمة `z` هي `2`.

2. التصريح عن متحول من نوع عدد عشري يكون بالشكل:

   ```cpp
   char x = 5.7;
   ```

3. نتيجة العملية الحسابية التالية هي `22`:

   ```cpp
   6 + 8 / 2 + 4 * 2
   ```

4. التعليمة التي تؤدي إلى تنفيذ مجموعة من التعليمات عددًا من المرات هي:

   ```cpp
   if
   ```

5. تعليمة القراءة والإدخال هي:

   ```cpp
   while
   ```

---

**السؤال 38:**
صحح الأخطاء الموجودة في البرنامج التالي، والذي يقوم بحساب مجموع الأعداد من `30` إلى `50`.

```cpp
#include <iostream.h>

main()
{
    char sum = 0;

    while (i = 1; i++; i <= 20)
    {
        sum == sum + 0;
    }

    cin << "sum is" << i;

    return 0;
}
```

---

**السؤال 39:**
اكتب بالطريقة البيانية الخوارزمية المناسبة لقراءة علامة طالب `mark`، في مادة، وطباعة العبارة `passed` إذا كان ناجحًا، وطباعة العبارة `failed` إذا كان راسبًا.
شرط النجاح هو الحصول على العلامة `60` أو أكثر.

---

**السؤال 40:**
اكتب نتيجة تنفيذ كلٍّ من المقاطع البرمجية التالية:

1.

```cpp
int x = 4, y = 11;

if (x % 2 == 0 && y > 10)
    cout << "value is" << x + y;
else
    cout << "The Val = " << x * y;
```

2.

```cpp
for (int i = 10; i < 15; i++)
{
    if (i == 13)
        break;

    cout << i << endl;
}
```

3.

```cpp
for (int i = 6; i > 1; i--)
{
    if (i % 2 != 0)
        continue;

    cout << i << "\t" << i * i << endl;
}
```

4.

```cpp
int num = 10;

cout << num++ << "\n";

cout << num;
```

---

**السؤال 41:**
اكتب برنامجًا يطلب من المستخدم إدخال `10` أعداد صحيحة، ثم يقوم بحساب ناتج جمع هذه الأعداد، فإذا كان الناتج أكبر من `100` وأصغر تمامًا من `200` يطبع العبارة `Between 100 and 200`، وإذا كان يساوي `500` يطبع العبارة `Equal 500`، وإذا كان غير ذلك يطبع العبارة `Try again`.

---

**السؤال 42:**
صحح الأخطاء الموجودة في البرنامج التالي، والذي يقوم بحساب مجموع الأعداد الزوجية الموجودة في المجال من `0` إلى `30`.

```cpp
#include <iostream.h>

main()
{
    char sum = 0;

    for (i = 1; i <= 15; i--)
    {
        if (i / 2 = 0)
            sum = sum + 1;
    }

    cin << "sum is : " << i;

    return 0;
}
```

---

**السؤال 43:**
اكتب نتيجة تنفيذ كلٍّ من المقاطع البرمجية التالية:

1.

```cpp
int a = 10, b;

b = a + 8 / 2;

cout << b << "\n";

if (b <= 9)
    cout << "values are between 0 and 15";
else
    cout << "values more than 15";
```

2.

```cpp
int z = 10;

cout << --z << "\n";

cout << z;

int y = 20;

cout << y << "\n";

cout << y;
```

---

**السؤال 44:**
أجب بـ `صح` أو `خطأ` عن كل مما يلي، مع تصحيح العبارات الخاطئة:

1. التصريح عن متحول من نوع عدد صحيح يكون بالشكل:

   ```cpp
   float z = 2.3;
   ```

2. ناتج تنفيذ العملية الحسابية التالية هو `6`:

   ```cpp
   6 + 2 * 4 / 4
   ```

3. نستخدم التعليمة `for` لتنفيذ مجموعة من التعليمات عدة مرات.

4. تعليمة الطباعة على الشاشة هي:

   ```cpp
   while
   ```

5. بفرض:

   ```cpp
   a = 2
   b = 5
   c = a % b
   ```

   فإن قيمة `c` هي `4`.

---

**السؤال 45:**
صحح الأخطاء الموجودة في البرنامج التالي، والذي يقوم بحساب المتوسط الحسابي للأعداد الموجودة في المجال من `1` إلى `10`.

```cpp
#include <iostream.h>

main()
{
    int sum = 1;

    for (int i = 1; i <= 10; i--)
    {
        sum = sum + 1;
    }

    cout << "avg = " << i;

    return 0;
}
```

---

**السؤال 46:**
أجب بـ `صح` أو `خطأ` عما يلي، مع تصحيح العبارة الخاطئة:

1. نستخدم الرمز `\n` لتحريك المؤشر مسافة جدولية أفقية.

2. التعليمة التالية تستخدم للتصريح عن متحول عشري:

   ```cpp
   char a;
   ```

3. بفرض:

   ```cpp
   x = 16
   y = 5
   z = x % y
   ```

   فإن قيمة `z` هي `3.5`.

4. نستخدم التعليمة `cout` لقراءة قيمة من المستخدم.

5. يمكن لاسم المتحول في `C++` أن يحتوي مسافة فارغة.

---

**السؤال 47:**
اكتب بالطريقة البيانية، أي الخوارزمية البيانية، الخوارزمية اللازمة لإدخال عدد صحيح `x` وطباعة العبارة `Positive` إذا كان هذا العدد موجبًا، والعبارة `Negative` إذا كان سالبًا.

---

**السؤال 48:**
صحح الأخطاء الموجودة في البرنامج التالي، والذي يقوم بقراءة نصف قطر دائرة، ثم يطبع قيمة قطرها ومحيطها ومساحتها.

```cpp
#include <iostream.h>

main()
{
    bool r;

    float p = 3.14;

    cin >> r

    cin << r * 2 << "\n";

    cout << 2 * p * R << "\n";

    cout << p * r * r << "\n";

    return 0;
}
```
